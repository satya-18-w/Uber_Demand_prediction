from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A ml model trained on nyc yellow taxidata that suggest the driver to move to the more profitable region',
    author='Satyajit',
    license='',
)
